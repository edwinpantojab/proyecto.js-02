// Math.js es una biblioteca de JavaScript de código abierto que proporciona una amplia gama de funciones matemáticas y herramientas para manipulación de datos en línea con la sintaxis de JavaScript. Permite a los desarrolladores realizar cálculos complejos, como cálculo simbólico, estadísticas, álgebra lineal, etc.

// A continuación se describen algunos de los métodos proporcionados por Math.js:

// Math.abs(): Devuelve el valor absoluto de un número.

// Math.ceil(): Redondea un número hacia arriba al entero más cercano.

// Math.floor(): Redondea un número hacia abajo al entero más cercano.

// Math.max(): Devuelve el número más grande en una lista de números.

// Math.min(): Devuelve el número más pequeño en una lista de números.

// Math.round(): Redondea un número al entero más cercano.

// Math.pow(): Devuelve el resultado de un número elevado a una potencia determinada.

// Math.sqrt(): Devuelve la raíz cuadrada de un número.

// Math.exp(): Devuelve el valor de la constante matemática e elevado a una potencia determinada.

// Math.log(): Devuelve el logaritmo natural de un número.

// Math.sin(): Devuelve el seno de un ángulo.

// Math.cos(): Devuelve el coseno de un ángulo.

// Math.tan(): Devuelve la tangente de un ángulo.

// Math.asin(): Devuelve el arcoseno de un número.

// Math.acos(): Devuelve el arcocoseno de un número.

// Math.atan(): Devuelve el arcotangente de un número.

// Además de estos métodos, Math.js también proporciona herramientas para realizar operaciones matriciales, álgebra lineal, cálculo simbólico y estadísticas. La biblioteca también admite la creación y manipulación de estructuras de datos complejas, como matrices y vectores.

// Math.js es una herramienta útil para desarrolladores que necesitan realizar cálculos matemáticos complejos en aplicaciones web. Su sintaxis similar a JavaScript lo hace fácil de aprender y utilizar. Además, la biblioteca es de código abierto y cuenta con una amplia documentación y comunidad de desarrolladores que la respaldan.

// Aquí hay algunos códigos utilizando el objeto Math en JavaScript:

// Obtener un número aleatorio entre 0 y 1:

const numeroAleatorio = Math.random();

//Redondear un número al entero más cercano:

const numer = 3.14;
const numeroRedondeado = Math.round(numero);

i//Obtener el valor absoluto de un número:

const numeroNegativo = -5;
const valorAbsoluto = Math.abs(numeroNegativo);

//Calcular la raíz cuadrada de un número:

const nume = 16;
const raizCuadrada = Math.sqrt(numero);

//Calcular el seno de un ángulo en radianes:

const angul = Math.PI / 4; // Ángulo de 45 grados en radianes
const seno = Math.sin(angulo);

//Calcular el coseno de un ángulo en radianes:

const angulo = Math.PI / 4; // Ángulo de 45 grados en radianes
const coseno = Math.cos(angulo);

//Calcular el valor mínimo de un conjunto de números:

const num = [1, 2, 3, 4, 5];
const minimo = Math.min(...numeros);

//Calcular el valor máximo de un conjunto de números:

const numeros = [1, 2, 3, 4, 5];
const maximo = Math.max(...numeros);

//Redondear un número hacia abajo al número entero más cercano:

const numero1 = 3.9;
const enter = Math.floor(numero1);

//Redondear un número hacia arriba al número entero más cercano:

const numero = 3.1;
const entero = Math.ceil(numero);



